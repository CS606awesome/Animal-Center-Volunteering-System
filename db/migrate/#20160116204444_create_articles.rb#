class CreateArticles < ActiveRecord::Migration
  def change
    reversible do |dir|
    create_table :articles do |t|

      dir.up   { t.change :title, :string  }
      dir.down { t.change :title, :integer } 
      t.text :text
      
      t.timestamps null: false
      end
    end
  end
end
