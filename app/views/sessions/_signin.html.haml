%fieldset
 %legend
 =form_for(:session, url: login_path) do |f|

  =f.text_field :email, {:placeholder => 'E-mail' }

  =f.text_field :password,{:placeholder => 'Password', :type=>'password'}
  =f.submit 'I am already a volunteer!'

