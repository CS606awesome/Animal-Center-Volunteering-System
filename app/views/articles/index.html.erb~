<h1>Hello, Rails!</h1>
<%= link_to 'New article', new_account_article_path %>
<h2>Listing articles</h2>


<table>
  <tr>
    <th>Title </th>
    <th>Text </th>
    <th colspan="2"></th>
  </tr>
  <% @article.each do |article| %>
  <tr>
   <td> <%= article.title %></td>
   <td><%= article.text %></td>
   <td><%= link_to 'Show', account_article_path(article)  %></td>
   <td><%= link_to 'Edit', edit_account__article_path(article)  %></td>
   <td><%= link_to 'Destroy', account_article_path(article),
	   method: :delete %></td>
  </tr>
  <% end %>
  </table>
